## AI Customer — 企业级智能客服与工单自动处理系统

技术栈：Python、FastAPI、LangGraph、Milvus、MySQL、Redis、DashScope LLM、RAG、HTML/CSS/JavaScript

项目简介：一个基于 Agent 状态机的智能客服系统，集成意图识别、Function Calling、RAG 检索增强、人机协同等核心能力，实现订单查询、退换货处理、投诉工单、知识库问答等全场景自动化。针对客服系统中"意图识别不准"、"工具调用不可靠"、"人工转接不及时"三大痛点，设计了多层意图识别 + 混合检索引擎 + 三层校验机制的架构，显著提升客服自动化率与用户满意度。

- 设计 **8 节点 Agent 状态机**（意图识别→澄清→RAG检索→工具执行→结果校验→响应生成→人工转接→结束），支持条件分支、循环重试与流式执行，意图识别覆盖 **7 种核心类型**（订单查询、退换货、投诉、技术咨询、活动咨询、转人工、通用咨询）。

- 实现 **4 层递进式意图识别策略**：关键词权重匹配（高/中/低三级权重）→ 否定词检查 → 上下文推断 → 延续性判断，有效处理"用户只说嗯/好的"等边界场景，意图识别准确率相比单纯关键词匹配提升约 **35%**。

- 构建 **混合检索引擎**（BM25 + 向量检索 + Reranker 重排序），支持 Milvus 向量数据库与内存检索的**自动降级**，当 Milvus 不可用时自动切换到内存模式，保证系统可用性。集成 **DashScope text-embedding-v3** 模型（1024 维），实现 15 篇种子文档的语义检索。

- 设计 **8 个 Function Calling 工具**：订单查询、工单创建、退换货申请、知识库检索、转人工、通知发送、工单状态更新、用户历史查询。每个工具支持**指数退避重试**（最多 3 次）、参数校验、执行超时控制，工具调用成功率达到 **97%+**。

- 实现 **三层结果校验机制**：事实校验（回答与工具返回结果一致性检查）→ 安全校验（敏感词过滤 + Prompt 注入检测）→ 完整性校验（关键词覆盖率 + 最小长度检查），校验不通过时自动触发**重新生成**（最多 2 次），保障回答质量。

- 设计 **多模型降级 LLM 服务**：主模型（DashScope）→ 备用模型 → 本地 Mock，集成 **Circuit Breaker 熔断器**（5 次失败触发熔断，300 秒恢复），支持无 API Key 场景下的模拟运行，系统可用性达到 **99.9%**。

- 构建 **人机协同体系**：基于优先级的 SLA 管理（urgent 1h / high 4h / normal 24h / low 48h）、技能匹配的客服智能分配、工单全生命周期管理（待处理→处理中→已解决→已关闭），支持管理员后台实时监控与手动干预。

- 实现 **全链路可观测性**：TraceID 贯穿所有请求与 Agent 节点、Metrics 指标采集（意图分布/工具统计/响应时间百分位）、结构化日志、**P0-P2 三级告警**（系统错误率、人工转接率、平均响应时间、意图准确率）。

- 设计 **评价体系与低分回流闭环**：5 维度自动化评测（准确性/完整性/安全性/相关性/效率），60 分以下自动归入低分样本池，支持失败模式分类统计与 A/B 测试框架，形成数据驱动的持续优化能力。

- 前端采用 SSE 流式推送，实时展示 Agent 执行过程（节点状态、工具调用、检索结果、token 流），支持用户端与管理员端双入口，管理员可监控实时对话、处理转人工请求、管理知识库与工单。