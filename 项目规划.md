
---

# 项目一：企业级智能客服与工单自动处理系统

> **定位**：成熟可落地，技术栈全面，有明确商业价值，覆盖JD中90%的必会+熟悉技能

## 一、项目背景与真实痛点

**业务场景**：中大型企业客服部门面临三大痛点：
1. 80%的咨询是重复问题（退换货政策、订单查询、使用教程），人工客服机械重复回答，人力成本高
2. 复杂问题需要跨部门流转（技术支持→仓储→财务），工单平均处理时长>24小时，用户满意度低
3. 客服人员水平参差不齐，回答准确率波动大，知识更新滞后

**目标用户**：企业客服团队 + 终端消费者
**可衡量效果**：问题解决率、平均响应时间、人工转接率、用户满意度（CSAT）、工单处理时长

## 二、系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端层 (Web/小程序/SDK)                │
├─────────────────────────────────────────────────────────┤
│                   API网关 (FastAPI + Nginx)              │
│         鉴权 │ 限流 │ 路由 │ WebSocket流式输出            │
├──────────┬──────────┬──────────┬────────────────────────┤
│ 会话管理  │ 意图识别  │ Agent编排│     知识管理层          │
│ (Redis)  │(分类模型) │(LangGraph)│  RAG引擎 + 向量数据库   │
├──────────┴──────────┴──────────┴────────────────────────┤
│                    工具层 (Function Calling)              │
│  订单查询 │ 工单创建 │ 退换货申请 │ 知识库检索 │ 人工转接  │
├─────────────────────────────────────────────────────────┤
│              数据层 (MySQL + Redis + Milvus)             │
├─────────────────────────────────────────────────────────┤
│         可观测性层 (OpenTelemetry + Prometheus + Grafana) │
│         评价层 (Ragas + 自定义评测 + 低分样本回流)         │
└─────────────────────────────────────────────────────────┘
```

## 三、核心技术栈

| 层级 | 技术选型 | 对应JD技能 |
|------|---------|-----------|
| 编程语言 | Python 3.11+ | ✅ 必会 |
| Agent框架 | LangGraph（状态机驱动的多节点Agent） | ✅ 高频 |
| RAG引擎 | 自研RAG Pipeline + LangChain Retriever | ✅ 必会 |
| 向量数据库 | Milvus（生产级分布式向量库） | ✅ 熟悉 |
| Embedding/Reranker | BGE-large-zh + BGE-Reranker-v2 | ✅ 熟悉 |
| 后端框架 | FastAPI + Pydantic + Uvicorn | ✅ 必会 |
| 关系数据库 | MySQL 8.0（用户、工单、会话、知识库元数据） | ✅ 必会 |
| 缓存/会话 | Redis（会话状态、限流、热点知识缓存） | ✅ 必会 |
| 消息队列 | RabbitMQ（异步工单处理、评价任务） | ✅ 熟悉 |
| 大模型 | GPT-4o-mini（主）+ DeepSeek-V3（备）+ 本地Qwen-7B（兜底降级） | ✅ 必会 |
| 意图识别 | 微调后的BERT分类模型（12类意图） | ✅ 熟悉 |
| Prompt管理 | 自研Prompt版本管理模块（支持A/B测试、灰度发布） | ✅ 熟悉 |
| 可观测性 | OpenTelemetry链路追踪 + Prometheus指标 + Grafana大盘 | ✅ 加分 |
| 部署 | Docker + Docker Compose + Nginx反向代理 | ✅ 熟悉 |
| 测试 | pytest + Locust压测 | ✅ 工程能力 |

## 四、Agent核心能力设计（对应要点3）

### 4.1 意图识别
- 第一层：轻量BERT分类模型做粗分类（12类：查询订单/退换货/投诉/技术咨询/活动咨询/转人工/...），延迟<50ms
- 第二层：对模糊意图，LLM做二次确认，通过追问澄清用户需求
- 意图置信度<0.7时，自动触发澄清对话

### 4.2 任务规划（LangGraph状态机）
```
用户输入 → 意图识别 → 【分支判断】
  ├─ 简单咨询 → RAG直接回答 → 结果校验 → 输出
  ├─ 订单操作 → 工具调用(查订单) → 结果校验 → 回答/转人工
  ├─ 退换货 → 多步工具链(查订单→校验规则→创建工单→通知) 
  ├─ 投诉升级 → 创建工单+自动分配+通知人工
  └─ 模糊需求 → 澄清追问 → 重新规划
```

### 4.3 工具选择（Function Calling）
实现8个结构化工具，每个工具包含：名称、描述、参数Schema、执行函数、失败处理策略
- `query_order(order_id)`：查询订单状态与物流
- `create_ticket(category, content, priority)`：创建工单并自动分配
- `apply_refund(order_id, reason)`：退换货申请（需规则校验）
- `search_kb(query, top_k)`：知识库检索
- `escalate_to_human(reason)`：转接人工客服
- `send_notification(channel, content)`：发送短信/站内信通知
- `update_ticket_status(ticket_id, status)`：更新工单状态
- `get_user_history(user_id)`：获取用户历史咨询记录

### 4.4 状态追踪
- 每个会话维护完整状态图：当前意图、已调用工具、已获取信息、待确认项、用户情绪分值
- 状态持久化到Redis，支持会话中断恢复（用户第二天回来继续上次问题）
- 多轮对话中自动追踪已确认信息，避免重复询问

### 4.5 失败重试与结果校验
- **工具调用失败**：指数退避重试（最多3次），超过阈值自动降级（如订单系统超时→告知用户稍后查询并创建工单）
- **回答结果校验**：三层校验机制
  1. 事实校验：回答中的关键信息（订单号、金额、日期）与工具返回结果一致性检查
  2. 安全校验：敏感词过滤、Prompt注入检测、越权操作拦截
  3. 完整性校验：是否回答了用户所有问题点（LLM-as-Judge打分）
- 校验不通过→自动重生成（最多2次）→仍失败→转人工

## 五、完整后端工程体系（对应要点2）

### 5.1 API接口设计（RESTful + WebSocket）
- `POST /api/v1/chat/send`：发送消息（支持流式SSE返回）
- `WS /api/v1/chat/stream`：WebSocket实时对话
- `POST /api/v1/tickets`：创建工单
- `GET /api/v1/tickets/{id}`：查询工单状态
- `POST /api/v1/knowledge/upload`：上传知识库文档
- `GET /api/v1/analytics/session`：会话统计
- `POST /api/v1/feedback`：用户评价反馈

### 5.2 数据库设计（核心表）
- `users`：用户信息（ID、昵称、等级、历史标签）
- `sessions`：会话记录（会话ID、用户ID、开始/结束时间、状态、CSAT评分）
- `messages`：消息记录（角色、内容、Token消耗、耗时、关联工具调用）
- `tickets`：工单（ID、分类、优先级、状态、分配人、SLA倒计时）
- `knowledge_docs`：知识库文档（ID、标题、分类、版本、更新时间、Chunk数量）
- `agent_traces`：Agent执行链路（TraceID、各节点耗时、工具调用参数与结果、最终输出）
- `evaluation_results`：评测结果（样本ID、维度得分、是否低分样本、处理状态）

### 5.3 会话管理
- 基于Redis的会话状态存储，TTL=24小时
- 支持多设备同步（同一用户Web端和小程序端会话共享）
- 会话摘要机制：超过20轮对话时自动生成摘要，压缩上下文，降低Token成本

### 5.4 日志追踪
- 全链路TraceID贯穿：API请求→意图识别→Agent节点→工具调用→LLM调用→响应输出
- 结构化日志（JSON格式），包含TraceID、SpanID、耗时、Token数、错误信息
- 日志分级：DEBUG（开发）/INFO（正常流程）/WARN（降级重试）/ERROR（异常）

### 5.5 错误兜底与降级策略
| 故障场景 | 降级策略 |
|---------|---------|
| 主LLM API超时/限流 | 自动切换备用模型（DeepSeek→本地Qwen） |
| 向量数据库不可用 | 降级为关键词检索（Elasticsearch BM25） |
| 订单系统接口失败 | 告知用户系统繁忙，自动创建工单由人工跟进 |
| 工具调用连续失败 | 终止自动处理，转人工并附带已收集的上下文 |
| 回答校验不通过 | 重生成→仍失败→转人工+标记为低分样本 |

### 5.6 多用户并发
- FastAPI异步架构 + Uvicorn多Worker
- Redis分布式锁防止同一用户重复创建工单
- 数据库连接池（SQLAlchemy + 连接池大小=20）
- LLM调用并发控制（信号量限制同时请求数，防止触发API限流）
- Locust压测验证：单节点支持200并发用户，P95响应时间<3秒

## 六、可观测性设计（对应要点4）

### 6.1 全链路追踪（OpenTelemetry）
- 每个请求生成唯一TraceID，贯穿所有组件
- 关键节点埋点：意图识别耗时、RAG检索耗时、每个工具调用耗时、LLM首Token延迟、总耗时
- Jaeger可视化展示调用链路，快速定位性能瓶颈

### 6.2 指标监控（Prometheus）
**Agent能力指标**：
- 意图识别准确率（按分类统计）
- 工具调用成功率/失败率（按工具类型）
- 任务一次完成率（无需人工介入）
- 多轮对话平均轮次
- 回答校验通过率

**业务指标**：
- 人工转接率（目标<20%）
- 平均响应时间（目标<3秒）
- 工单平均处理时长
- 用户满意度CSAT（目标>4.2/5）

**成本指标**：
- 日均Token消耗量
- 单次对话平均成本
- 各模型调用占比
- 缓存命中率

### 6.3 可视化大盘（Grafana）
- 实时大盘：当前在线用户、对话量、转人工率、异常告警
- 趋势大盘：近7天/30天的解决率、满意度、成本趋势
- 下钻分析：按意图分类、按用户等级、按时间段的细粒度统计

### 6.4 告警机制
- P0告警（电话+短信）：系统不可用、错误率>5%、LLM全部降级
- P1告警（企业微信）：人工转接率>30%、平均响应>5秒、Token日消耗超预算20%
- P2告警（邮件）：某意图识别准确率<70%、低分样本量突增

## 七、评价体系（对应要点5）

### 7.1 多维度自动化评测
**离线评测**（每日定时跑，500+标注样本）：
| 维度 | 评测方法 | 权重 |
|------|---------|------|
| 回答准确率 | LLM-as-Judge + 标准答案对比 | 35% |
| 检索召回率 | Top-K召回率（标注相关文档） | 20% |
| 工具调用正确率 | 工具选择+参数准确性 | 20% |
| 回答完整性 | 是否覆盖用户所有问题点 | 15% |
| 安全性 | 敏感内容/越权/幻觉检测 | 10% |

**在线评测**（实时）：
- 用户点赞/点踩（隐式+显式反馈）
- 对话结束后CSAT评分（1-5星）
- 转人工率作为负向指标（自动处理失败的代理指标）
- 用户追问率（回答不清楚导致用户重复提问）

### 7.2 低分样本回流闭环
```
低分样本检测(自动评测<60分 或 用户点踩)
    → 自动存入低分样本池(标注失败原因分类)
    → 每周自动聚类分析(找出高频失败模式)
    → 人工审核+标注(每天处理50条)
    → 反哺优化：
        ├─ 知识库补充(知识缺失类)
        ├─ Prompt优化(回答质量类)
        ├─ 工具修复(工具调用错误类)
        ├─ 意图分类样本扩充(识别错误类)
        └─ 加入回归测试集(防止优化回退)
    → 优化后重新评测，验证提升
```

### 7.3 A/B测试框架
- Prompt版本、模型选择、检索策略均可配置A/B实验
- 按用户ID哈希分流，自动统计实验组与对照组的指标差异
- 显著性检验（p<0.05）后决定是否全量发布

## 八、人机协同设计（对应要点6）

### 8.1 人在系统中的三个位置

**位置1：人工客服接管（前端介入）**
- 触发条件：用户主动要求转人工 / Agent连续失败 / 高优先级投诉 / 涉及金额>500元的复杂售后
- 接管时自动传递完整上下文：会话历史、已调用工具结果、用户情绪分析、建议解决方案
- 人工处理过程中，Agent作为Copilot辅助：实时推荐知识库答案、自动填写工单、生成回复草稿

**位置2：知识库运营（后端介入）**
- 运营人员通过后台管理知识库：文档上传、审核、分类、版本管理
- 低分样本自动推送给运营人员，提示"该问题知识库无相关内容，建议补充"
- 知识更新后自动触发增量索引，无需全量重建

**位置3：系统管理员（监控介入）**
- 通过Grafana大盘监控系统健康状态
- 异常告警时人工介入排查
- 配置调整：限流阈值、降级策略、SLA规则、Prompt灰度比例

### 8.2 人机协作的关键时机
- **Agent处理中**：每一步操作可被人工打断和纠正
- **Agent不确定时**：置信度低的操作（如退款审批）自动暂停，等待人工确认
- **Agent完成后**：复杂工单自动生成处理建议，人工审核后执行
- **持续学习**：人工客服的优质回复自动收集，作为Few-shot示例反哺Prompt优化

## 九、量化成果（简历用）

- 独立设计并实现完整的Agent客服系统，支持**12类意图**识别、**8个工具**调用、多步任务自动执行
- 混合检索（向量+BM25）+ Reranker策略，Top-3知识召回率从72%提升至**91%**
- 三层结果校验机制将回答幻觉率从18%降至**4%**
- 全链路可观测性覆盖，问题定位时间从平均30分钟缩短至**<5分钟**
- 自动化评测体系覆盖500+样本，低分样本回流闭环使月度解决率提升**15%**
- 单节点支持**200并发**，P95响应时间**<3秒**，三级降级策略保障系统可用性**99.9%**
- 在模拟测试集上实现**78%问题自动解决**，人工转接率降低至**22%**，单次对话平均成本**¥0.08**
